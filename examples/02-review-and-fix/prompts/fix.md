A reviewer found problems with the implementation in this repository. Fix them.

Rules:

- **Do not modify, delete, or add any file under `acceptance/`.** If a test disagrees with your code, the code is wrong.
- No dependencies, no installs.
- Run `node --test acceptance/*.test.js` after your changes and keep working until every test passes.
- Address the findings below. If a finding is wrong, say so in your summary and explain why — but still make the test suite green.

Reply with a short summary of what you changed and the final test result.

## Reviewer findings

{{verdict}}

## Specification

{{spec}}
